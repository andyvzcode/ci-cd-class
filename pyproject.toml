[project]
name = "python-app-ci"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.8",
    "fastapi>=0.121.1",
    "fastapi[standard]>=0.121.1",
    "pytest>=9.0.1",
    "ruff>=0.14.5",
]

[dependency-groups]
dev = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
